// object

    const person ={
        name: 'Igor',
        age: 18,
        weight: 52.00,
        isAdmin: true
    }

    //objeto inteiro
    console.log(person)

    // uma parte
    console.log(person.name)

    //interpolação
    console.log(`${person.name} tem ${person.age} anos`)